query
{
  devices{name, state
  }
  device(name: "dserver/CentralNode/test") {
    name
    state
    connected
    deviceClass
    pid
    startedDate
    stoppedDate
    exported
    commands{name}
  }
}