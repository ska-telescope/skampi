{{ if .Values.kibana.enabled }}

apiVersion: v1
kind: Service
metadata:
  name: kibana-{{ template "logging.name" . }}-{{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    service: kibana
spec:
  type: NodePort
  selector:
    run: kibana
  ports:
  - port: 5601
    targetPort: 5601

{{ end }}
